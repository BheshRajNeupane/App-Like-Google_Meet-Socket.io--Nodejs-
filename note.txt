!important --> to over write css , properties over boostrap


window.location.origin--> givers orgin url like https://facebook.com , http://127.0.0.1:5500/


.video-wrap>*  --> select video-wrap class and applid {..} all properties


-------------------------app.js file.................

1.MyAPP fxn return function object _init which hold function(uid,mid) in which init fxn is called .

2.





...........server js file......

1.const io= require('socket.io')(server); --return io fxn where server pass as argument to connet express server and socket

2.app.use(express.static(path.join(__dirname,""))); --> render client side from server side , this point to main dir to render static file.

if  (__dirname,"public/"))--> point to render client side from public folder


3. socket error in clint side
  
  note : all error can be seacrh on socket.io and solve by readinf docs,code

:3000/socket.io/?EIO=3&transport=polling&t=Oioa5MX:1  
        Failed to load resource: the server responded with a status of 400 (Bad Request)


->click this error link in we page and goto network section and see error msg 
{
    "code": 5,
    "message": "Unsupported protocol version"
} 
-->search erro in socket documents and solve ..
const io = require("socket.io")(httpServer, {
  allowEIO3: true // false by default
});


4.

